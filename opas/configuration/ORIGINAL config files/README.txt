These configuration files are currently read-only.  When copying them over for use, the read-only attribute must be first unchecked.

Also, all tuning curve paths in the .ini files must be updated to reflect the correct installation directory!